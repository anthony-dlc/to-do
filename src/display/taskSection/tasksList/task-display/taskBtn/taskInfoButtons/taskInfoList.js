// function taskInfoList(project, task) {
//   const listContainer = document.createElement("ul");
//   listContainer.classList.add("task-info-list");

//   const eliminateTask = document.createElement("li");
//   const editTask = document.createElement("li");
//   // eliminateTask recibe el boton de eliminar task
//   // editTask recibe el boton de editar task

//   eliminateTask.append(eliminateTask(project, task));
//   editTask.append(editTask(project, task));

//   listContainer.append(eliminateTask);
//   listContainer.append(editTask);

//   return listContainer;
// }
